{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
"startCommand": "cd handyman && python manage.py migrate --noinput && daphne -b 0.0.0.0 -p $PORT handyman.asgi:application", 
   "restartPolicyType": "ON_FAILURE"
  }
}
