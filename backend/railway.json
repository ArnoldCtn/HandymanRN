{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c \"python manage.py collectstatic --noinput && gunicorn handyman.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
